import { YoungDirective } from './young.directive';

describe('YoungDirective', () => {
  it('should create an instance', () => {
    const directive = new YoungDirective();
    expect(directive).toBeTruthy();
  });
});
